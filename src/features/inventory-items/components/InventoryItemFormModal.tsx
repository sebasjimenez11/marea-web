import { Modal } from '../../../components/ui/Modal';

export function InventoryItemFormModal() {
  return (
    <Modal title="Producto" open={false} onClose={() => undefined}>
      Formulario pendiente de implementar.
    </Modal>
  );
}
